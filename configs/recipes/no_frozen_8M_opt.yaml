# Parameter-efficient training: freezes all FastESM parameters except multimer_attn
# Trains only ~0.5% of total parameters
# Pretrained weights auto-download from Synthyra HF Hub
# Recommended for quick experiments and prototyping

extends: ../base/8M.yaml

model:
  copy_weights: true
  try_opt: true  # try architectural optimizations, but its a bit different than original MINT
  use_multimer: true
  layer_spec: ['self','self','cross','self','self','cross','self','self','cross']
  per_chain_rope: true

training:
  lr: 0.0004
  max_steps: 400000
  batch_size: 200 #128 # 128 and larger is possible for try_opt = true, 24 barely fits for try_opt = false
  accumulate_grad: 1
  val_check_interval: 2000
  fp8: true
  

data:
  val_examples: 20000
  num_workers: 8
  data_dir: ./data/diamond

output:
  run_name: 8M_no_frozen_opt
  checkpoint_every: 2000
